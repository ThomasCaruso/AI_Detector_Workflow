[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "authorship-shift"
version = "0.8.0"
description = "A portable writing interface and provider-agnostic research engine for controlled text transformation with semantic fidelity."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Thomas Caruso"}]
dependencies = []

[project.scripts]
authorship-shift = "authorship_shift.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
